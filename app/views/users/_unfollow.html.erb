<%= form_with(model: Current.user.active_relationships.find_by(followed_id: user.id),
            method: :delete) do |f| %>
  <%= f.submit "フォロー解除", class: "px-4 py-1.5 rounded-full bg-white border border-gray-300 text-gray-700 text-sm font-bold hover:bg-gray-50 transition cursor-pointer" %>
<% end %>
