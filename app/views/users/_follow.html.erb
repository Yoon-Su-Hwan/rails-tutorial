<%= form_with(model: Current.user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, user.id %></div>
  <%= f.submit "フォローする", class: "px-4 py-1.5 rounded-full bg-blue-600 text-white text-sm font-bold hover:bg-blue-700 transition cursor-pointer" %>
<% end %>
